{
  "buildCommand": "if [ ! -d _flutter ]; then git clone https://github.com/flutter/flutter.git --depth 1 -b stable _flutter; fi && ./_flutter/bin/flutter build web --release",
  "outputDirectory": "build/web",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}