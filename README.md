# HydroMesh FloodNet Twin

## Setup Instructions

### Prerequisites
- Node.js v20+
- Flutter 3.16+
- PostgreSQL 16 with PostGIS
- [Any other requirements]

### Installation
1. Clone repository: `git clone [url]`
2. Install backend dependencies: `cd backend && npm install`
3. Install frontend dependencies: `cd frontend && flutter pub get`
4. Setup database: `[instructions]`
5. Configure environment: `cp .env.example .env`

### Running the Project
1. Start backend: `npm run start`
2. Start frontend: `flutter run`

### Test Credentials (if needed)
- Email: test@example.com
- Password: test123
